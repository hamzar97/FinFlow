server:
  port: 8703

spring:
  application:
    name: transaction
  data:
    mongodb:
      host: localhost
      port: 27017
      database: FinFlow
  doc:
    api-docs:
      path: /api-docs

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true